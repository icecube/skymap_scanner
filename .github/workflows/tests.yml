name: tests

on: [push]

jobs:

  test-build-docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          cd cloud_tools/
          cp --remove-destination ../python/prepare_frames.py .
          docker build --no-cache -t skyscan-cloud .