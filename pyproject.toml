# pyproject.toml

[build-system] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
requires = ["setuptools>=78.1", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
dependencies = [
  'healpy',
  'icecube-skyreader>=1.4.2',
  'iminuit<2.27.0',
  'numpy<2.0.0',
  'oms-mqclient',
  'scipy',
  'wipac-dev-tools',
  'wipac-rest-tools'
] # originally migrated from 'setup.cfg' by 'WIPACrepo/wipac-dev-py-setup-action' github action package
dynamic = ["version"] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
name = "skymap-scanner" # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
requires-python = ">=3.9, <3.14" # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
keywords = [
  'WIPAC',
  'IceCube',
  'Skymap Scanner',
  'Reconstruction',
  'IceTray',
  'EWMS'
] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)

[project.optional-dependencies] # originally migrated from 'setup.cfg' by 'WIPACrepo/wipac-dev-py-setup-action' github action package
pulsar = [
  'oms-mqclient[pulsar]'
]
rabbitmq = [
  'oms-mqclient[rabbitmq]'
]
nats = [
  'oms-mqclient[nats]'
]
all = [
  'oms-mqclient[all]'
]
mypy = [
  'oms-mqclient[all]',
  'oms-mqclient[nats]',
  'oms-mqclient[pulsar]',
  'oms-mqclient[rabbitmq]'
] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)

[tool.setuptools.package-data]
"*" = ["py.typed"] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)

[tool.setuptools.packages.find] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
namespaces = false
exclude = ["test", "tests", "doc", "docs", "resource", "resources", "example", "examples"]

[tool.setuptools_scm] # note: no changes allowed (autogenerated by 'WIPACrepo/wipac-dev-py-setup-action' github action package)
